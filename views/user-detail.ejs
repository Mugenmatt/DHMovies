<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Usuario</title>
    <link rel='stylesheet' href='/stylesheets/user-detail.css' />
</head>
<body>
    <header>

        <% if(locals.usuario) { %> 
          <form class="form-logout" action="/users" method="POST">
          
            <button class="logout" type="submit">Cerrar Sesión</button>
          
          </form>
        <% } %> 
  
      </header>

      <main class="fade-in">

    <ul>
        <li> ID: <%= usuarioDetalle.id %>  </li>
        <li> Email: <%= usuarioDetalle.email %> </li>
        <li> Rol: <%= usuarioDetalle.rol %> </li>
      </ul>

    <form action="/users/database/user-detail/<%= usuarioDetalle.id %>/delete" method="POST">
    
      <button type="submit">Borrar Usuario</button>

    </form>

    <a class="cambiarPagina" href="/users/database/user-detail/<%= usuarioDetalle.id %>/update-rol">Actualizar Rol</a>

    <a class="cambiarPagina" href="/users/database">Volver al listado de Usuarios Cinefilos</a>
    <a class="cambiarPagina" href="/">Volver a la Home</a>
  </main>
</body>
</html>