<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/register.css' />
  </head>
  <body>

    <header>
    
    </header>
<main class="fade-in">
    <h1><%= title %></h1>

    <form action="/users/register" method="POST">
    
        <label for="email">Email</label>
        <input type="email" name="email" id="email" value="<%= typeof dataAntesIngresada != 'undefined' ? dataAntesIngresada.email : '' %> ">
        <p class="error">
            <% if(typeof errors != 'undefined') { %>
                <% if(errors.email) { %>
                    <%= errors.email.msg %> 
                <% } %>  
            <% } %>  
        </p>

        <label for="pass">Password</label>
        <input type="password" name="password" id="pass">
        <p class="error">
            <% if(typeof errors != 'undefined') { %>
                <% if(errors.password) { %>
                    <%= errors.password.msg %> 
                <% } %>  
            <% } %>  
        </p>

        <label for="pass">Confirme su contraseña</label>
        <input type="password" name="confirmPassword" id="pass">
        <p class="error">
            <% if(typeof errors != 'undefined') { %>
                <% if(errors.confirmPassword) { %>
                    <%= errors.confirmPassword.msg %> 
                <% } %>  
            <% } %>  
        </p>

        <label for="rol">Rol</label>
        <select name="rol" id="rol">
            <option value="0">Usuario</option>
            <option value="1">Admin</option>
        </select>

        <input type="submit" value="Registrate">

    </form>

    <a class="cambiarPagina" href="/users/login">¿Ya tenes un usuario?</a>

    <a class="cambiarPagina" href="/">Volver a Home</a>
</main>
</body>
</html>
